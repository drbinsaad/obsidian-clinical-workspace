/**
 * Replaced at build time by esbuild's `define`. Release builds substitute
 * `false`, so every development-only branch is removed by dead-code
 * elimination and the synthetic fixtures never reach `dist/main.js`.
 */
declare const __DEV_TOOLS__: boolean;
